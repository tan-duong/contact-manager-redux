const initState = {}

const contactReducer = (state = initState, action) => {
    switch(action.type){
        case 'ADD_CONTACT':
        default:
            return state
    }
}

export default contactReducer